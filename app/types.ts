export default interface Course {
  title: string;
  description: string;
  imageUrl: string;
  id: number;
}

export default interface Article {
  title: string;
  description: string;
  imageUrl: string;
  id: number;
}
